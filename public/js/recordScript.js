import { fetchEventData, displayEventDetails } from './eventsAdmin.js';

// Use the imported functions where needed
// For example:
fetchEventData(); // Call the function to fetch event data
displayEventDetails(eventData, numberOfDays, selectedDay);
